api - enginx -> microservice
add read replica -> with DB proxy/load balancer.
add the logegr service wise and global means central level
add caching central level
add idempotency
add cap therom
add jwt token authentcation
imlemettion of api gateway 




how api ateway ane inginex work 

the work diffrrenlty or work work together 

og is it like if we use enginx then we don't need to use api gateway , or if we use api gateway then we don't need to use enginx 



